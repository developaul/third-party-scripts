name: Labeler

on: [pull_request]

jobs:
  labeler:
    runs-on: ubuntu-latest
    name: Label the PR
    steps: 
      - name: Pull request size labeler
        uses: developaul/pr-size-labeler@v1.0.3
        with: 
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
